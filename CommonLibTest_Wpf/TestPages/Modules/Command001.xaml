<Page x:Class="CommonLibTest_Wpf.TestPages.Modules.Command001"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CommonLibTest_Wpf.TestPages.Modules"
      d:DataContext="{d:DesignInstance local:Command001ViewModel}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Command001">

    <StackPanel Orientation="Vertical">
        <Button Command="{Binding C1}" Height="30">C1 指令</Button>
        <Button Command="{Binding C2}" Height="30">C2 指令</Button>

        <Separator></Separator>
        
        <Button Command="{Binding C3}" Height="30">C3 指令</Button>
        <Button Command="{Binding C4}" Height="30">C4 指令</Button>

        <Separator></Separator>
        
    </StackPanel>
</Page>
